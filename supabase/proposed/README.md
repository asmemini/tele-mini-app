# Proposed schema only

These SQL files document later Magster database changes for Telegram identity and Mini App customization.

They have **not** been applied to the existing Magster Supabase project (`mrzmhtirmxqnnoqppnyf`).

Do not apply them until:

1. The 4-step registration flow is being implemented.
2. Admin Panel customization for the Mini App is being implemented.

Existing Magster tables (`courses`, `bundles`, `bundle_courses`, `students`, `payment_requests`, `app_payment_methods`, `student_access`, `app_settings`) must remain the source of truth.
